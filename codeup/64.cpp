#include<iostream>
using namespace std;

int main(){
	cin.tie(NULL); 
	ios_base::sync_with_stdio(false);

	while(1){
		int num;
		cin >> num;
		if(num == 0) break;
		cout << num << "\n";
	}
			
	return 0;
}
